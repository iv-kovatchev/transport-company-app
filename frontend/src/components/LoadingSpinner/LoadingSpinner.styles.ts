import type { SxProps, Theme } from '@mui/material';

const loadingSpinnerStyles: SxProps<Theme> = {
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  height: '100vh',
};

export default loadingSpinnerStyles;